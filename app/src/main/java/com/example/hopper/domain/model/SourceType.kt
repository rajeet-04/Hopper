package com.example.hopper.domain.model

/**
 * Classification indicating the origin of pandal data,
 * used to assess data trustworthiness.
 */
enum class SourceType {
    COMMITTEE,
    VOLUNTEER,
    NEWS,
    UNKNOWN
}
