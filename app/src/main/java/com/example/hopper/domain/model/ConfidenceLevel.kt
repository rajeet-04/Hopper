package com.example.hopper.domain.model

/**
 * Three-tier rating indicating the reliability of a data entry
 * based on its source and verification status.
 */
enum class ConfidenceLevel {
    LOW,
    MEDIUM,
    HIGH
}
